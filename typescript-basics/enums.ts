let Enum = "read-only object with fixed values + type safety";
enum UserRoles {
  ADMIN = "admin",
  GUEST = "guest",
  "SUPER_ADMIN" = "super_admin",
}
let a: string | number;


function num() : object{
  return {
    name:"hari"
  };
}

